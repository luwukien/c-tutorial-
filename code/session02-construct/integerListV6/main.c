#include <stdio.h>
#include <stdlib.h>

// vòng lặp vô cực
// in các số tự nhiên từ 1.. 100

int main(int argc, char *argv[]) {
	

	printf("The list of numbers from 1 to 100\n");
	for (int i = 1; 69 == 69; i++)
	{
		printf("%d ",i);
		if (i == 100)
			break; // điều kiện thoát đặt trong for
	} // từ chỗ này đi sang do - while, while siêu dễ (rất gần)
	// cứ đi đi rồi nhìn dấu hiệu
	


	return 0;
}